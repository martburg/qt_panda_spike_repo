# Log viewer defaults
#
# Usage:
#   python -m steuerung3d.apps.log_viewer path/to/session.jsonl --config configs/log_viewer.toml

[view]
# from_tick = 0
# to_tick = 200
# every = 1
# axes = ["X", "Y"]
show_pos = true

diff --git a/configs/debug/lifrtick.toml b/configs/debug/lifrtick.toml
deleted file mode 100644
index 5417a0f..0000000
--- a/configs/debug/lifrtick.toml
+++ /dev/null
@@ -1,21 +0,0 @@
-# Lifetick tracing ("oscilloscope" style) for debugging connectivity.
-#
-# This creates a dedicated rotating log file for high-frequency lifetick
-# information without flooding the main core logger.
-#
-# Enable intentionally defaults to false.
-
-[lifrtick]
-enable = false
-
-# Throttle: log at most every N seconds per axis.
-every_s = 0.05
-
-# Output log path (relative to CWD). A good default is the session folder.
-path = ".run/lifrtick.log"
-
-# Rotation settings:
-#   max_bytes   - max file size before rollover
-#   backup_count - number of rollover files to keep
-max_bytes = 5_000_000
-backup_count = 10
diff --git a/configs/debug/livetick.toml b/configs/debug/livetick.toml
deleted file mode 100644
index 22a0b1f..0000000
--- a/configs/debug/livetick.toml
+++ /dev/null
@@ -1,21 +0,0 @@
-# Lifetick tracing ("oscilloscope" style) for debugging connectivity.
-#
-# This creates a dedicated rotating log file for high-frequency lifetick
-# information without flooding the main core logger.
-#
-# Enable intentionally defaults to false.
-
-[lifetick]
-enable = false
-
-# Throttle: log at most every N seconds per axis.
-every_s = 0.05
-
-# Output log path (relative to CWD). A good default is the session folder.
-path = ".run/lifetick.log"
-
-# Rotation settings:
-#   max_bytes   - max file size before rollover
-#   backup_count - number of rollover files to keep
-max_bytes = 5_000_000
-backup_count = 10
diff --git a/conftest.py b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/conftest.py
index 835fb4e..04fbc59 100644
--- a/conftest.py
+++ b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/conftest.py
@@ -15,8 +15,6 @@ SRC = Path(__file__).resolve().parent / "src"
 if SRC.exists():
     sys.path.insert(0, str(SRC))
 import importlib.util
-import os
-
 import pytest
 
 
@@ -24,17 +22,6 @@ def _has_pyside6() -> bool:
     return importlib.util.find_spec("PySide6") is not None
 
 
-def _has_transitions() -> bool:
-    return importlib.util.find_spec("transitions") is not None
-
-
 def pytest_runtest_setup(item: pytest.Item) -> None:
     if "ui" in item.keywords and not _has_pyside6():
         pytest.skip("PySide6 not installed; skipping UI test")
-
-    # Optional dependency gate: only run when explicitly opted-in.
-    if "requires_transitions" in item.keywords:
-        if os.environ.get("RUN_TRANSITIONS_TESTS", "0") != "1":
-            pytest.skip("transitions tests are opt-in; set RUN_TRANSITIONS_TESTS=1")
-        if not _has_transitions():
-            pytest.skip("transitions not installed")
diff --git a/docs/protocols/legacy_plc_anton.md b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_anton.md
index 4f0194c..57f0d77 100644
--- a/docs/protocols/legacy_plc_anton.md
+++ b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_anton.md
@@ -1,4 +1,6 @@
-# Legacy PLC UDP Protocol — Anton (172.16.17.2)
+# Legacy PLC UDP Protocol — Anton
+
+Shared guarantees: `legacy_plc_common.md` (172.16.17.2)
 
 Canonical source: Beckhoff ST program `KommAnton__MAIN.st`.
 
diff --git a/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_burt.md b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_burt.md
new file mode 100644
index 0000000..13b647a
--- /dev/null
+++ b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_burt.md
@@ -0,0 +1,13 @@
+# Legacy PLC UDP Protocol — Burt
+
+This axis uses the **same payload contract** as Anton.
+
+See:
+- `legacy_plc_common.md` (shared guarantees)
+- `legacy_plc_anton.md` (field-by-field reference)
+
+## Endpoints
+
+- PLC IP: `172.16.17.4`
+- PLC bind: `172.16.17.4:15001`
+- Controller bind: `172.16.17.5:15002`
diff --git a/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_cecil.md b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_cecil.md
new file mode 100644
index 0000000..4d2b1e3
--- /dev/null
+++ b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_cecil.md
@@ -0,0 +1,13 @@
+# Legacy PLC UDP Protocol — Cecil
+
+This axis uses the **same payload contract** as Anton.
+
+See:
+- `legacy_plc_common.md` (shared guarantees)
+- `legacy_plc_anton.md` (field-by-field reference)
+
+## Endpoints
+
+- PLC IP: `172.16.17.1`
+- PLC bind: `172.16.17.1:15001`
+- Controller bind: `172.16.17.5:15002`
diff --git a/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_common.md b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_common.md
new file mode 100644
index 0000000..a4d7ac4
--- /dev/null
+++ b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_common.md
@@ -0,0 +1,16 @@
+# Legacy PLC UDP Protocol (Common)
+
+This page documents the **shared** Beckhoff PLC UDP protocol shape used by the legacy controller.
+
+All payloads are ASCII, `;`-delimited, and **order-sensitive**.
+
+## Contract guarantees
+
+- Delimiter `;`, usually a trailing `;` is present.
+- Decimal separator is `.`
+- `Intent` is the string `"True"` / `"False"` (not a boolean).
+- Parsing is positional: **do not reorder fields**.
+
+## Axis-specific pages
+
+The per-axis pages only differ by **addresses / endpoint tuples**.
diff --git a/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_debby.md b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_debby.md
new file mode 100644
index 0000000..40a057c
--- /dev/null
+++ b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_debby.md
@@ -0,0 +1,13 @@
+# Legacy PLC UDP Protocol — Debby
+
+This axis uses the **same payload contract** as Anton.
+
+See:
+- `legacy_plc_common.md` (shared guarantees)
+- `legacy_plc_anton.md` (field-by-field reference)
+
+## Endpoints
+
+- PLC IP: `172.16.17.3`
+- PLC bind: `172.16.17.3:15001`
+- Controller bind: `172.16.17.5:15002`
diff --git a/docs/protocols/legacy_plc_protocols.md b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_protocols.md
index c39ee8e..2f6f96f 100644
--- a/docs/protocols/legacy_plc_protocols.md
+++ b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/protocols/legacy_plc_protocols.md
@@ -10,9 +10,13 @@ All payloads are ASCII `;`-delimited and order-sensitive.
 | Axis / PLC | PLC IP | PLC bind | Controller bind | Doc |
 |---|---:|---|---|---|
 | Anton | 172.16.17.2 | 172.16.17.2:15001 | 172.16.17.5:15002 | `legacy_plc_anton.md` |
-| Debby | 172.16.17.3 | 172.16.17.3:15001 | 172.16.17.5:15002 | `legacy_plc_debby.md` (TODO) |
-| Burt  | 172.16.17.4 | 172.16.17.4:15001 | 172.16.17.5:15002 | `legacy_plc_burt.md` (TODO) |
-| Cecil | 172.16.17.1 | 172.16.17.1:15001 | 172.16.17.5:15002 | `legacy_plc_cecil.md` (TODO) |
+| Debby | 172.16.17.3 | 172.16.17.3:15001 | 172.16.17.5:15002 | `legacy_plc_debby.md` |
+| Burt  | 172.16.17.4 | 172.16.17.4:15001 | 172.16.17.5:15002 | `legacy_plc_burt.md` |
+| Cecil | 172.16.17.1 | 172.16.17.1:15001 | 172.16.17.5:15002 | `legacy_plc_cecil.md` |
+
+## Shared contract
+
+See: `legacy_plc_common.md`
 
 ## Contract guarantees (shared)
 
diff --git a/docs/refactor/plans/refos-plan__livetick-rename__LATEST.md b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/refactor/plans/refos-plan__livetick-rename__LATEST.md
index c018784..26e451a 100644
--- a/docs/refactor/plans/refos-plan__livetick-rename__LATEST.md
+++ b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/docs/refactor/plans/refos-plan__livetick-rename__LATEST.md
@@ -1,9 +1,7 @@
-# REFOS Plan — Lane 1 Rename: lifrtick → livetick (LATEST)
+# OBSOLETE — lived "livetick" rename plan
 
-See [refos-plan__livetick-rename__20260227_1145__v01.md](refos-plan__livetick-rename__20260227_1145__v01.md) for full details.
+This plan is **obsolete**.
 
-## Summary
-- All “lifrtick” occurrences will be renamed to “livetick” (config, code, docs, tests, filenames).
-- Lane 1 only: No semantic or runtime changes.
-- Atomic commit required to avoid partial rename states.
-- Protecting tests and gates listed in versioned plan.
+Canonical naming is **lifetick**.
+
+See: `refos-plan__lifrtick-to-lifetick__LATEST.md`.
diff --git a/pyproject.toml b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/pyproject.toml
index e16825b..a6a1d1f 100644
--- a/pyproject.toml
+++ b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/pyproject.toml
@@ -20,5 +20,9 @@ package-dir = {"" = "src"}
 where = ["src"]
 
 [tool.pytest.ini_options]
+markers = [
+    "ui: tests requiring PySide6/Qt",
+]
+addopts = "-ra"
 # Tests live at repo root.
 testpaths = ["tests"]
diff --git a/pytest.ini b/pytest.ini
deleted file mode 100644
index 398476c..0000000
--- a/pytest.ini
+++ /dev/null
@@ -1,6 +0,0 @@
-[pytest]
-testpaths = tests
-addopts = -ra
-markers =
-    ui: tests requiring PySide6/Qt
-    requires_transitions: tests requiring the optional 'transitions' dependency (set RUN_TRANSITIONS_TESTS=1)
diff --git a/src/steuerung3d/config/lifrtick_config.py b/src/steuerung3d/config/lifrtick_config.py
deleted file mode 100644
index 1a856d1..0000000
--- a/src/steuerung3d/config/lifrtick_config.py
+++ /dev/null
@@ -1,77 +0,0 @@
-from __future__ import annotations
-
-from dataclasses import dataclass
-from pathlib import Path
-from typing import Any
-
-from steuerung3d.config.toml_loader import load_toml
-
-
-@dataclass(frozen=True)
-class LifetickTraceConfig:
-    """Config for lifetick tracing ("oscilloscope" style) output.
-
-    The intent is to optionally stream high-frequency Lifetick events into a
-    dedicated rotating log file without flooding the main core logger.
-    """
-
-    enable: bool = False
-    every_s: float = 0.5
-
-    # File output. If *path* is relative, it is interpreted relative to CWD.
-    path: str = ".run/lifrtick.log"
-
-    # Rotation settings.
-    max_bytes: int = 2_000_000
-    backup_count: int = 5
-
-
-def load_lifetick_config(path: Path) -> LifetickTraceConfig:
-    """Load lifetick trace config from TOML.
-
-    Missing file -> defaults.
-    Invalid fields -> best-effort defaults.
-    """
-
-    if not path.exists():
-        return LifetickTraceConfig()
-
-    data = load_toml(path)
-    # Historical/legacy spelling: [lifrtick]
-    root = data.get("lifrtick", {}) if isinstance(data, dict) else {}
-    if not isinstance(root, dict):
-        return LifetickTraceConfig()
-
-    def _get_bool(key: str, default: bool) -> bool:
-        v = root.get(key, default)
-        return bool(v)
-
-    def _get_float(key: str, default: float) -> float:
-        v = root.get(key, default)
-        try:
-            return float(v)
-        except Exception:
-            return float(default)
-
-    def _get_int(key: str, default: int) -> int:
-        v = root.get(key, default)
-        try:
-            return int(v)
-        except Exception:
-            return int(default)
-
-    def _get_str(key: str, default: str) -> str:
-        v: Any = root.get(key, default)
-        return str(v) if v is not None else str(default)
-
-    every_s = max(0.0, _get_float("every_s", LifetickTraceConfig.every_s))
-    max_bytes = max(1, _get_int("max_bytes", LifetickTraceConfig.max_bytes))
-    backup_count = max(0, _get_int("backup_count", LifetickTraceConfig.backup_count))
-
-    return LifetickTraceConfig(
-        enable=_get_bool("enable", LifetickTraceConfig.enable),
-        every_s=every_s,
-        path=_get_str("path", LifetickTraceConfig.path),
-        max_bytes=max_bytes,
-        backup_count=backup_count,
-    )
diff --git a/tests/test_axis_fsm.py b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/tests/test_axis_fsm.py
index 880f375..92bf1f2 100644
--- a/tests/test_axis_fsm.py
+++ b/mnt/data/repo/dev-chore-docs-dedupe-rawstring-rename/tests/test_axis_fsm.py
@@ -1,12 +1,6 @@
 import pytest
 
 
-pytest.importorskip("transitions")
-
-
-pytestmark = pytest.mark.requires_transitions
-
-
 from steuerung3d.core.axis_fsm import AxisFSM, AxisFsmConfig, ST_IDLE, ST_ESTOP, ST_RECOVER
 from steuerung3d.core.axis_types import AxisTelemetry, AxisRequest
 

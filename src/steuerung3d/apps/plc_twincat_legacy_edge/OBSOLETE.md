# Obsolete: plc_twincat_legacy_edge

This edge app is kept for legacy TwinCAT UDP roundtrip/debug workflows.

**Status:** obsolete / retained for reference.

New work should prefer profile-driven stacks (`python -m steuerung3d up --profile ...`) and the
canonical PLC protocol path under `src/steuerung3d/adapters/plc/` + `src/steuerung3d/protocol/`.

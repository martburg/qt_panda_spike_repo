# Obsolete: core_service

`configs/stacks/*` profiles (including `1dev_sim`) use `steuerung3d.apps.core_udp_service`.

This older `core_service` entry point is retained for reference during the transition period.
New work should target `core_udp_service`.

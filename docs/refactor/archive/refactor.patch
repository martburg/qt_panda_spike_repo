diff --git a/src/steuerung3d/adapters/plc/plc_codec.py b/src/steuerung3d/adapters/plc/plc_codec.py
index 54dbd4c..f0b9723 100644
--- a/src/steuerung3d/adapters/plc/plc_codec.py
+++ b/src/steuerung3d/adapters/plc/plc_codec.py
@@ -29,6 +29,9 @@ class PlcCodec:
     """
 
     spec: PlcWireSpec
+    # Optional label for diagnostics/logging (e.g. endpoint name).
+    # NOTE: This must not affect wire compatibility.
+    axis_id: str = ""
 
     # -----------------------
     # TX: core -> PLC
diff --git a/src/steuerung3d/apps/plc_stack/builder.py b/src/steuerung3d/apps/plc_stack/builder.py
index f10ddc5..15fa0d3 100644
--- a/src/steuerung3d/apps/plc_stack/builder.py
+++ b/src/steuerung3d/apps/plc_stack/builder.py
@@ -34,7 +34,7 @@ class LinkFactory(Protocol):
 
 
 class CodecFactory(Protocol):
-    def __call__(self, *, spec: PlcWireSpec): ...  # pragma: no cover
+    def __call__(self, *, spec: PlcWireSpec, axis_id: str): ...  # pragma: no cover
 
 
 # --------------------
@@ -98,7 +98,7 @@ def build_plc_device(
     if link_factory is None:
         link_factory = lambda *, bind, target: UdpLink(bind=bind, target=target)
     if codec_factory is None:
-        codec_factory = lambda *, spec: PlcCodec(spec=spec)
+        codec_factory = lambda *, spec, axis_id: PlcCodec(spec=spec, axis_id=axis_id)
 
     endpoints: list[PlcEndpoint] = []
     for ep_cfg in cfg.plc_endpoints:
@@ -110,8 +110,11 @@ def build_plc_device(
             true_token=ep_cfg.true_token,
             false_token=ep_cfg.false_token,
         )
-        codec = codec_factory(spec=spec)
-        link = link_factory(bind=(ep_cfg.bind_host, ep_cfg.bind_port), target=(ep_cfg.target_host, ep_cfg.target_port))
+        codec = codec_factory(spec=spec, axis_id=str(ep_cfg.name))
+        link = link_factory(
+            bind=(ep_cfg.bind_host, ep_cfg.bind_port),
+            target=(ep_cfg.target_host, ep_cfg.target_port),
+        )
 
         endpoints.append(
             PlcEndpoint(
diff --git a/src/steuerung3d/docs/legacy/steuerung3d_docs/plc_stack.md b/src/steuerung3d/docs/legacy/steuerung3d_docs/plc_stack.md
index fe1f0a2..4b801c3 100644
--- a/src/steuerung3d/docs/legacy/steuerung3d_docs/plc_stack.md
+++ b/src/steuerung3d/docs/legacy/steuerung3d_docs/plc_stack.md
@@ -39,7 +39,7 @@ Builds the PLC edge adapter:
 
 - for each configured endpoint:
   - `UdpLink(bind=(...), target=(...))`
-  - `PlcCodec(spec=PlcWireSpec(...))`
+  - `PlcCodec(spec=PlcWireSpec(...), axis_id=endpoint_name)`
   - `PlcEndpoint(name, axis_ids, link, codec)`
 - validates endpoint/axis ownership
 - returns `(MultiPlcDevice, endpoints)`
diff --git a/src/steuerung3d/docs/plc_stack.md b/src/steuerung3d/docs/plc_stack.md
index fe1f0a2..4b801c3 100644
--- a/src/steuerung3d/docs/plc_stack.md
+++ b/src/steuerung3d/docs/plc_stack.md
@@ -39,7 +39,7 @@ Builds the PLC edge adapter:
 
 - for each configured endpoint:
   - `UdpLink(bind=(...), target=(...))`
-  - `PlcCodec(spec=PlcWireSpec(...))`
+  - `PlcCodec(spec=PlcWireSpec(...), axis_id=endpoint_name)`
   - `PlcEndpoint(name, axis_ids, link, codec)`
 - validates endpoint/axis ownership
 - returns `(MultiPlcDevice, endpoints)`
diff --git a/src/steuerung3d/protocol/udp_plc_channels.py b/src/steuerung3d/protocol/udp_plc_channels.py
index ebafcca..e53ba12 100644
--- a/src/steuerung3d/protocol/udp_plc_channels.py
+++ b/src/steuerung3d/protocol/udp_plc_channels.py
@@ -1,10 +1,10 @@
 from __future__ import annotations
 
-"""UDP channels for legacy TwinCAT semicolon-delimited PLC frames.
+r"""UDP channels for legacy TwinCAT semicolon-delimited PLC frames.
 
 These are intentionally *not* JSON. They carry plain ASCII lines like:
 
-    0;20400;0;0;0.0;...;Anton;...;EOD\;...
+    0;20400;0;0;0.0;...;Anton;...;EOD\\;...
 
 The canonical field order is defined in :mod:`steuerung3d.protocol.legacy_plc`.
 
diff --git a/tests/unit/test_plc_device_builder.py b/tests/unit/test_plc_device_builder.py
index e36db21..91a5258 100644
--- a/tests/unit/test_plc_device_builder.py
+++ b/tests/unit/test_plc_device_builder.py
@@ -20,8 +20,8 @@ class FakeUdpLink:
 
 class FakeCodec:
     def __init__(self, *, spec, axis_id: str):
-         self.spec = spec
-    self.axis_id = axis_id
+        self.spec = spec
+        self.axis_id = axis_id
 
 
 def test_build_plc_device_constructs_endpoints_without_opening_sockets(tmp_path: Path) -> None:
@@ -56,7 +56,8 @@ axis_ids = ["Y", "Z"]
     device, endpoints = build_plc_device(
         cfg,
         link_factory=lambda *, bind, target: FakeUdpLink(bind=bind, target=target),
-        codec_factory=lambda *, spec, axis_id: FakeCodec(spec=spec, axis_id=axis_id),    )
+        codec_factory=lambda *, spec, axis_id: FakeCodec(spec=spec, axis_id=axis_id),
+    )
 
     assert len(endpoints) == 2
     assert endpoints[0].name == "Anton"
